type Query {
    find(pageNum: Int!, pageSize: Int!): [EventDTOResponse!]!
}

enum EventStatus {
    OPEN
    CLOSED
    ON_GOING
    FULL
    CANCELED
    COMPLETED
}

type EventDTOResponse{
    id: ID!
    capacity: Int!
    eventStatus: EventStatus!
}